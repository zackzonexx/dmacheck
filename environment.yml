name: production

channels:
  - default
  - conda-forge  # Add conda-forge channel for opsgenie-sdk

dependencies:
  - python=3.10*  # Assuming Python 3.10 or compatible (adjust if needed)
  - opsgenie-sdk  # From conda-forge
  - datadog
  - requests
  - urllib3
  - pytest
